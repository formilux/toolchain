The "patches" directory is classed by project name and version.
gcc-20011006 represents the latest CVS version of gcc-2.95.

Some patches were collected from external sources :
  - gcc2.95-mega.patch.bz2
    a collection of patches from Erik Andersen to fix remaining known bugs in
    gcc-2.95.

  - glibc-2.3.2-csu-Makefile.patch
    a patch from GOTO Masanori to fix invalid code generation under some shells

Other patches of interest :
  - gcc-2.95-gcc3-compfix.diff.bz2
    fix an ifdef'd out comment which breaks build under host gcc >= 3

